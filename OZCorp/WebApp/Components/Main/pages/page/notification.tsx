import * as React from 'react';
import Notification from '../../containers/alerts/notification';
const notification = () => (
    <div className="container">
        <Notification />
    </div>
);

export default notification;