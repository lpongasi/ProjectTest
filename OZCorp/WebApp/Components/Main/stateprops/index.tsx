export const jobOrder = (state) => (
    {
        jobOrder: state.jobOrder
    }
);
export const notification = (state) => (
    {
        notification: state.notification
    }
);
export const report = (state) => (
    {
        report: state.report
    }
);
export const homeSetting = (state) => (
    {
        homeSetting: state.homeSetting
    }
);
export const manageItem = (state) => (
    {
        manageItem: state.manageItem
    }
);
export const manageUser = (state) => (
    {
        manageUser: state.manageUser
    }
);
export const warehouse = (state) => (
    {
        warehouse: state.warehouse
    }
);

export const purchaseReport = (state) => (
    {
        purchaseReport: state.purchaseReport
    }
);

export const companyPurchaseReport = (state) => (
    {
        companyPurchaseReport: state.companyPurchaseReport
    }
);
